{
  "name": "basys-contract-approval-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "End-to-end тесты (Playwright) для примера ContractApproval на платформе BaSYS",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@types/node": "^20.11.0",
    "dotenv": "^16.4.5"
  }
}
